import {
  createChannel,
  getChannelById,
  getChannelByName,
  updateChannelNameById,
  deleteChannel
} from './channel';

import { createStream, getStreamById, deleteStream, updateAnalytics } from './stream';

export {
  createChannel,
  getChannelById,
  getChannelByName,
  updateChannelNameById,
  deleteChannel,
  createStream,
  getStreamById,
  deleteStream,
  updateAnalytics
};
